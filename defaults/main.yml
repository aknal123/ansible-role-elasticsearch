---
elasticsearch_network_host: "{{ inventory_hostname }}"
elasticsearch_http_port: 9200
elasticsearch_script_inline: true
elasticsearch_script_indexed: true
elasticsearch_path_data: /usr/share/elasticsearch/data
elasticsearch_path_logs: /var/log/elasticsearch/elasticsearch
#elasticsearch_discovery_zen_ping_unicast_hosts: ["{{ inventory_hostname }}"] # multiple hostnames separated by comma
elasticsearch_discovery_zen_minimum_master_nodes: 1
node_max_local_storage_nodes: 3
